.DS_Store
node_modules/
dist/
*.tsbuildinfo
docs/api/

# Local git worktrees (e.g. .worktrees/ralph/*) — keep them out of the working
# tree so an untracked dir doesn't dirty `git status` and block release checks.
.worktrees/

# Environment files with secrets
.env
.env.local
.env.staging
.env.production
